<div class="container">
    <h1 class="text-center">Valley</h1>
    {{search-form onSearch=(action "onSearch") query=currentQuery}}

    {{#each model as |searchResult|}}
        {{search-result data=searchResult}}
    {{/each}}
</div>

